import { defaultCalendar } from 'moment/src/lib/locale/calendar';
import { defaultLongDateFormat } from 'moment/src/lib/locale/formats';
import { defaultInvalidDate } from 'moment/src/lib/locale/invalid';
import { defaultOrdinal, defaultDayOfMonthOrdinalParse } from 'moment/src/lib/locale/ordinal';
import { defaultRelativeTime } from 'moment/src/lib/locale/relative';

// months
import {
    defaultLocaleMonths,
    defaultLocaleMonthsShort,
} from 'moment/src/lib/units/month';

// week
import { defaultLocaleWeek } from 'moment/src/lib/units/week';

// weekdays
import {
    defaultLocaleWeekdays,
    defaultLocaleWeekdaysMin,
    defaultLocaleWeekdaysShort,
} from 'moment/src/lib/units/day-of-week';

// meridiem
import { defaultLocaleMeridiemParse } from 'moment/src/lib/units/hour';

export var baseConfig = {
    calendar: defaultCalendar,
    longDateFormat: defaultLongDateFormat,
    invalidDate: defaultInvalidDate,
    ordinal: defaultOrdinal,
    dayOfMonthOrdinalParse: defaultDayOfMonthOrdinalParse,
    relativeTime: defaultRelativeTime,

    months: defaultLocaleMonths,
    monthsShort: defaultLocaleMonthsShort,

    week: defaultLocaleWeek,

    weekdays: defaultLocaleWeekdays,
    weekdaysMin: defaultLocaleWeekdaysMin,
    weekdaysShort: defaultLocaleWeekdaysShort,

    meridiemParse: defaultLocaleMeridiemParse
};
